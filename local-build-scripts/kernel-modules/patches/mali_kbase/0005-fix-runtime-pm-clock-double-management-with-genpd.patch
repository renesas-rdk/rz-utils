--- a/drivers/gpu/arm/midgard/platform/devicetree/mali_kbase_runtime_pm.c	2026-09-17 15:47:12.529809200 +0700
+++ b/drivers/gpu/arm/midgard/platform/devicetree/mali_kbase_runtime_pm.c	2026-09-17 15:47:24.222741400 +0700
@@ -54,7 +54,7 @@
 		if (WARN_ON(kbdev->clocks[i] == NULL))
 			;
 		else if (!__clk_is_enabled(kbdev->clocks[i]))
-			WARN_ON(clk_prepare_enable(kbdev->clocks[i]));
+			WARN_ON(clk_enable(kbdev->clocks[i]));
 	}
 }
 
@@ -66,7 +66,7 @@
 		if (WARN_ON(kbdev->clocks[i] == NULL))
 			;
 		else if (__clk_is_enabled(kbdev->clocks[i])) {
-			clk_disable_unprepare(kbdev->clocks[i]);
+			clk_disable(kbdev->clocks[i]);
 			WARN_ON(__clk_is_enabled(kbdev->clocks[i]));
 		}
 	}
